<Window x:Class="ZKTecoManager.CopyUsersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:ZKTecoManager.Controls"
        Title="نسخ المستخدمين بين الأجهزة - Copy Users Between Devices" Height="750" Width="750"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft">

    <Window.Resources>
        <!-- Primary Button -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI, Tahoma, Arial"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#667EEA" Offset="0"/>
                                    <GradientStop Color="#764BA2" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="#667EEA" Opacity="0.4" ShadowDepth="0" BlurRadius="12"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#5568D3" Offset="0"/>
                                            <GradientStop Color="#6A3E8F" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#9CA3AF"/>
                                <Setter TargetName="border" Property="Effect" Value="{x:Null}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Success Button (Green) -->
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#10B981" Offset="0"/>
                                    <GradientStop Color="#059669" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="#10B981" Opacity="0.3" ShadowDepth="0" BlurRadius="10"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#0EA06E" Offset="0"/>
                                            <GradientStop Color="#047857" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#9CA3AF"/>
                                <Setter TargetName="border" Property="Effect" Value="{x:Null}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Cancel Button -->
        <Style x:Key="CancelButton" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI, Tahoma, Arial"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F3F4F6"/>
                                <Setter Property="Foreground" Value="#374151"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Window Control Button -->
        <Style x:Key="WinBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#40FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Checkbox Style -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="FontFamily" Value="Segoe UI, Tahoma, Arial"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="White">
        <Border.Effect>
            <DropShadowEffect Color="#1F2937" Opacity="0.3" ShadowDepth="5" BlurRadius="25"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Gradient Header -->
            <Border Grid.Row="0" CornerRadius="15,15,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#667EEA" Offset="0"/>
                        <GradientStop Color="#764BA2" Offset="0.5"/>
                        <GradientStop Color="#F093FB" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Grid>
                    <!-- Title -->
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Right"
                                Margin="25,0"
                                FlowDirection="LeftToRight">
                        <TextBlock Text="📋" FontSize="24" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <TextBlock Text="نسخ المستخدمين بين الأجهزة"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="White"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Close Button -->
                    <Button Style="{StaticResource WinBtn}"
                            Content="✕"
                            Click="CloseButton_Click"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Margin="15,0"
                            FlowDirection="LeftToRight"/>
                </Grid>
            </Border>

            <!-- Main Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <Grid Margin="30,25,30,30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Source Device Info -->
                    <Border Grid.Row="0" Background="#E8F5E9" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="📱 الجهاز المصدر (نسخ منه)" FontSize="16" FontWeight="Bold" Foreground="#10B981" Margin="0,0,0,10"/>
                                <TextBlock x:Name="SourceDeviceText" Text="..." FontSize="14" Foreground="#374151"/>
                            </StackPanel>
                            <Button Grid.Column="1" x:Name="LoadUsersButton" Content="📥 تحميل المستخدمين"
                                    Style="{StaticResource PrimaryButton}" Height="40" Width="160"
                                    Click="LoadUsersFromDevice_Click"/>
                        </Grid>
                    </Border>

                    <!-- Employee Selection -->
                    <Border Grid.Row="1" x:Name="EmployeeSelectionPanel" Background="#FFF7ED" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                        <StackPanel>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <TextBlock Text="👤 اختيار المستخدمين" FontSize="16" FontWeight="Bold" Foreground="#EA580C"/>
                                    <TextBlock x:Name="UserCountText" Text=" (0 مستخدم)" FontSize="14" Foreground="#9A3412" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Content="تحديد الكل" Style="{StaticResource CancelButton}"
                                            Height="30" Width="90" Margin="0,0,10,0"
                                            Click="SelectAllUsers_Click"/>
                                    <Button Content="إلغاء الكل" Style="{StaticResource CancelButton}"
                                            Height="30" Width="90"
                                            Click="DeselectAllUsers_Click"/>
                                </StackPanel>
                            </Grid>

                            <TextBlock x:Name="NoUsersText" Text="اضغط 'تحميل المستخدمين' لقراءة المستخدمين من الجهاز"
                                       FontSize="13" Foreground="#9A3412" FontStyle="Italic" Margin="0,10,0,0"/>

                            <ScrollViewer x:Name="UsersScrollViewer" MaxHeight="150" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                                <StackPanel x:Name="SourceUsersPanel"/>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>

                    <!-- Target Device Selection -->
                    <Border Grid.Row="2" Background="#F0F4FF" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="🎯 الأجهزة الهدف (نسخ إليها)" FontSize="16" FontWeight="Bold" Foreground="#667EEA" Margin="0,0,0,10"/>
                            <TextBlock Text="اختر جهاز أو أكثر لنسخ المستخدمين إليها:" FontSize="13" Foreground="#6B7280" Margin="0,0,0,10"/>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <Button Content="تحديد الكل" Style="{StaticResource CancelButton}"
                                        Height="30" Width="100" Margin="0,0,10,0"
                                        Click="SelectAll_Click"/>
                                <Button Content="إلغاء التحديد" Style="{StaticResource CancelButton}"
                                        Height="30" Width="100"
                                        Click="DeselectAll_Click"/>
                            </StackPanel>

                            <ScrollViewer MaxHeight="120" VerticalScrollBarVisibility="Auto">
                                <StackPanel x:Name="TargetDevicesPanel"/>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>

                    <!-- Options -->
                    <Border Grid.Row="3" Background="#F9FAFB" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="⚙️ خيارات النسخ" FontSize="16" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>

                            <CheckBox x:Name="CopyFingerprintsCheckBox" Style="{StaticResource ModernCheckBox}"
                                      IsChecked="True" Content="👆 نسخ البصمات مع المستخدمين"/>

                            <CheckBox x:Name="OverwriteExistingCheckBox" Style="{StaticResource ModernCheckBox}"
                                      IsChecked="False" Content="🔄 استبدال المستخدمين الموجودين مسبقاً"/>

                            <CheckBox x:Name="CopyPasswordsCheckBox" Style="{StaticResource ModernCheckBox}"
                                      IsChecked="True" Content="🔐 نسخ كلمات المرور"/>
                        </StackPanel>
                    </Border>

                    <!-- Progress Area -->
                    <Border Grid.Row="4" x:Name="ProgressPanel" Background="#FEF3C7" CornerRadius="12" Padding="20"
                            Margin="0,0,0,15" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock x:Name="ProgressText" Text="جاري النسخ..." FontSize="14" FontWeight="SemiBold" Foreground="#92400E" Margin="0,0,0,10"/>
                            <ProgressBar x:Name="CopyProgressBar" Height="10" Minimum="0" Maximum="100" Value="0"/>
                            <TextBlock x:Name="ProgressDetailText" Text="" FontSize="12" Foreground="#78716C" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Action Buttons -->
                    <Grid Grid.Row="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Content="إلغاء"
                                Style="{StaticResource CancelButton}"
                                Click="CloseButton_Click"
                                Margin="0,0,10,0"/>

                        <Button Grid.Column="1" x:Name="CopyButton" Content="📋 بدء النسخ"
                                Style="{StaticResource SuccessButton}"
                                Click="StartCopy_Click"
                                Margin="10,0,0,0"/>
                    </Grid>
                </Grid>
            </ScrollViewer>

            <!-- Loading Overlay -->
            <controls:LoadingOverlay x:Name="LoadingOverlay" Grid.RowSpan="2"/>
        </Grid>
    </Border>
</Window>
