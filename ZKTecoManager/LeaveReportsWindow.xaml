<Window x:Class="ZKTecoManager.LeaveReportsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تقارير الإجازات - Leave Reports" Height="700" Width="1000"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="CanResize"
        MinWidth="800"
        MinHeight="550"
        FlowDirection="RightToLeft"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="#F3F4F6">
        <Border.Effect>
            <DropShadowEffect Color="#1F2937" Opacity="0.2" ShadowDepth="5" BlurRadius="20"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="55"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" CornerRadius="15,15,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#F59E0B" Offset="0"/>
                        <GradientStop Color="#D97706" Offset="0.5"/>
                        <GradientStop Color="#B45309" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="20,0" FlowDirection="LeftToRight">
                        <TextBlock Text="📄" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="تقارير الإجازات - Leave Reports" FontSize="17" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Button Content="✕" Click="CloseButton_Click"
                            HorizontalAlignment="Left" VerticalAlignment="Center"
                            Margin="12,0" Width="32" Height="32"
                            Background="Transparent" Foreground="White"
                            BorderThickness="0" Cursor="Hand" FontSize="14"/>
                </Grid>
            </Border>

            <!-- Filters -->
            <Border Grid.Row="1" Background="White" Padding="20,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Report Type -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,20,0">
                        <TextBlock Text="نوع التقرير:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
                        <ComboBox x:Name="ReportTypeComboBox" Width="180" Height="36" SelectionChanged="ReportTypeComboBox_SelectionChanged">
                            <ComboBoxItem Content="ملخص الأرصدة" Tag="balance_summary" IsSelected="True"/>
                            <ComboBoxItem Content="استخدام الإجازات" Tag="usage"/>
                            <ComboBoxItem Content="الإجازات الساعية" Tag="hourly"/>
                            <ComboBoxItem Content="كل العمليات" Tag="all_transactions"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Department Filter -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,20,0">
                        <TextBlock Text="القسم:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
                        <ComboBox x:Name="DepartmentComboBox" Width="160" Height="36">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DeptName}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>

                    <!-- Year -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,20,0">
                        <TextBlock Text="السنة:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
                        <ComboBox x:Name="YearComboBox" Width="90" Height="36"/>
                    </StackPanel>

                    <!-- Month (for some reports) -->
                    <StackPanel x:Name="MonthFilterPanel" Grid.Column="3" Orientation="Horizontal" Margin="0,0,20,0" Visibility="Collapsed">
                        <TextBlock Text="الشهر:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
                        <ComboBox x:Name="MonthComboBox" Width="120" Height="36">
                            <ComboBoxItem Content="الكل" Tag="0" IsSelected="True"/>
                            <ComboBoxItem Content="يناير" Tag="1"/>
                            <ComboBoxItem Content="فبراير" Tag="2"/>
                            <ComboBoxItem Content="مارس" Tag="3"/>
                            <ComboBoxItem Content="أبريل" Tag="4"/>
                            <ComboBoxItem Content="مايو" Tag="5"/>
                            <ComboBoxItem Content="يونيو" Tag="6"/>
                            <ComboBoxItem Content="يوليو" Tag="7"/>
                            <ComboBoxItem Content="أغسطس" Tag="8"/>
                            <ComboBoxItem Content="سبتمبر" Tag="9"/>
                            <ComboBoxItem Content="أكتوبر" Tag="10"/>
                            <ComboBoxItem Content="نوفمبر" Tag="11"/>
                            <ComboBoxItem Content="ديسمبر" Tag="12"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Actions -->
                    <StackPanel Grid.Column="5" Orientation="Horizontal" FlowDirection="LeftToRight">
                        <Button Click="GenerateReportButton_Click" Background="#10B981" Style="{StaticResource ActionButton}" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔍" Margin="0,0,6,0"/>
                                <TextBlock Text="عرض"/>
                            </StackPanel>
                        </Button>
                        <Button Click="ExportPdfButton_Click" Background="#EF4444" Style="{StaticResource ActionButton}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📥" Margin="0,0,6,0"/>
                                <TextBlock Text="PDF"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Report Content -->
            <Border Grid.Row="2" Background="White" Margin="15" CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Opacity="0.05" ShadowDepth="2" BlurRadius="8"/>
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Report Header -->
                    <Border Grid.Row="0" Background="#F9FAFB" CornerRadius="12,12,0,0" Padding="15,10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="ReportTitleText" Text="ملخص أرصدة الإجازات" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock x:Name="ReportSubtitleText" Text="" FontSize="13" Foreground="#6B7280" Margin="15,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- DataGrid -->
                    <DataGrid x:Name="ReportDataGrid"
                              Grid.Row="1"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              BorderThickness="0"
                              Background="White"
                              RowBackground="White"
                              AlternatingRowBackground="#F9FAFB"
                              FontSize="13"
                              SelectionMode="Single">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F3F4F6"/>
                                <Setter Property="Foreground" Value="#374151"/>
                                <Setter Property="Padding" Value="12,10"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#E5E7EB"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="12,8"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#DBEAFE"/>
                                        <Setter Property="Foreground" Value="#1F2937"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>
                    </DataGrid>

                    <!-- Empty State -->
                    <StackPanel x:Name="EmptyState" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                        <TextBlock Text="📋" FontSize="48" HorizontalAlignment="Center"/>
                        <TextBlock Text="اختر نوع التقرير واضغط عرض" FontSize="16" Foreground="#6B7280" Margin="0,15,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Loading Overlay -->
            <Border x:Name="LoadingOverlay" Grid.RowSpan="3" Background="#80FFFFFF" Visibility="Collapsed">
                <TextBlock Text="جاري إنشاء التقرير..." FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
        </Grid>
    </Border>
</Window>
