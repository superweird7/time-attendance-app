<Window x:Class="ZKTecoManager.BackupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="النسخ الاحتياطي والأمان - Backup &amp; Security" Height="700" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft">

    <Window.Resources>
        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" Margin="5,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#667EEA"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#667EEA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#667EEA" Offset="0"/>
                                    <GradientStop Color="#764BA2" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#5568D3" Offset="0"/>
                                            <GradientStop Color="#6A3E8F" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#10B981" Offset="0"/>
                                    <GradientStop Color="#059669" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#0EA06E" Offset="0"/>
                                            <GradientStop Color="#047857" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WarningButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#F59E0B" Offset="0"/>
                                    <GradientStop Color="#D97706" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#DC8B05" Offset="0"/>
                                            <GradientStop Color="#B45309" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#DC2626" Offset="0"/>
                                    <GradientStop Color="#B91C1C" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#B91C1C" Offset="0"/>
                                            <GradientStop Color="#991B1B" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WinBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#40FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Border CornerRadius="15" Background="White">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.3" ShadowDepth="5" BlurRadius="20"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" CornerRadius="15,15,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#667EEA" Offset="0"/>
                            <GradientStop Color="#764BA2" Offset="0.5"/>
                            <GradientStop Color="#F093FB" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>

                    <Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="25,0" FlowDirection="LeftToRight">
                            <TextBlock Text="💾" FontSize="22" Margin="0,0,10,0"/>
                            <TextBlock Text="النسخ الاحتياطي والأمان" FontSize="18" FontWeight="Bold" Foreground="White"/>
                        </StackPanel>

                        <Button Content="✕" Style="{StaticResource WinBtn}" Click="CloseButton_Click" HorizontalAlignment="Left" Margin="15,0"/>
                    </Grid>
                </Border>

                <!-- Tabs -->
                <TabControl Grid.Row="1" Margin="20,15,20,20" Background="Transparent" BorderThickness="0">
                    <!-- Backup/Restore Tab -->
                    <TabItem Header="النسخ الاحتياطي والاستعادة" FontSize="14" FontWeight="SemiBold">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="5">
                            <StackPanel Margin="15">
                                <!-- Backup Section -->
                                <Border Background="#F9FAFB" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                                    <StackPanel>
                                        <TextBlock Text="إنشاء نسخة احتياطية" FontSize="16" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>

                                        <TextBlock Text="مسار الحفظ:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBox x:Name="BackupPathTextBox" Grid.Column="0" Style="{StaticResource ModernTextBox}" Margin="0,0,10,0"/>
                                            <Button Grid.Column="1" Content="📁 استعراض" Style="{StaticResource PrimaryButton}" Width="120" Click="BrowseBackupPath_Click"/>
                                        </Grid>

                                        <Button Content="💾 إنشاء نسخة احتياطية الآن" Style="{StaticResource SuccessButton}" Margin="0,15,0,0" Click="CreateBackup_Click"/>
                                    </StackPanel>
                                </Border>

                                <!-- Restore Section -->
                                <Border Background="#FFF7ED" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                                    <StackPanel>
                                        <TextBlock Text="⚠️ استعادة من نسخة احتياطية" FontSize="16" FontWeight="Bold" Foreground="#D97706" Margin="0,0,0,15"/>

                                        <TextBlock Text="ملف النسخة الاحتياطية:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBox x:Name="RestoreFileTextBox" Grid.Column="0" Style="{StaticResource ModernTextBox}" Margin="0,0,10,0"/>
                                            <Button Grid.Column="1" Content="📁 اختيار ملف" Style="{StaticResource PrimaryButton}" Width="120" Click="BrowseRestoreFile_Click"/>
                                        </Grid>

                                        <Grid Margin="0,15,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Button Grid.Column="0" Content="🔍 التحقق من الملف" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0" Click="VerifyBackup_Click"/>
                                            <Button Grid.Column="1" Content="⚠️ استعادة البيانات" Style="{StaticResource WarningButton}" Margin="10,0,0,0" Click="RestoreBackup_Click"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <!-- Last Backup Info -->
                                <Border Background="#E8F5E9" CornerRadius="12" Padding="20">
                                    <StackPanel>
                                        <TextBlock Text="معلومات النسخ الاحتياطي" FontSize="14" FontWeight="Bold" Foreground="#059669" Margin="0,0,0,10"/>
                                        <TextBlock x:Name="LastBackupText" Text="آخر نسخة احتياطية: لا توجد" FontSize="13" Foreground="#374151"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- Settings Tab -->
                    <TabItem Header="الإعدادات" FontSize="14" FontWeight="SemiBold">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="5">
                            <StackPanel Margin="15">
                                <!-- Local Backup Settings -->
                                <Border Background="#F9FAFB" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                                    <StackPanel>
                                        <TextBlock Text="إعدادات النسخ الاحتياطي المحلي" FontSize="16" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>

                                        <CheckBox x:Name="AutoBackupCheckBox" Content="تفعيل النسخ الاحتياطي التلقائي المحلي" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,15" Checked="AutoBackupCheckBox_Changed" Unchecked="AutoBackupCheckBox_Changed"/>

                                        <Grid Margin="0,0,0,15">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                                <TextBlock Text="وقت النسخ الاحتياطي اليومي:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                                <TextBox x:Name="BackupTimeTextBox" Style="{StaticResource ModernTextBox}" Text="10:00"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                                <TextBlock Text="عدد أيام الاحتفاظ بالنسخ:" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                                <TextBox x:Name="RetentionDaysTextBox" Style="{StaticResource ModernTextBox}" Text="30"/>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <!-- Server Backup Settings -->
                                <Border Background="#EFF6FF" CornerRadius="12" Padding="20" BorderBrush="#93C5FD" BorderThickness="1" Margin="0,0,0,15">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                            <TextBlock Text="🖥️" FontSize="20" Margin="0,0,10,0"/>
                                            <TextBlock Text="إعدادات النسخ الاحتياطي على السيرفر" FontSize="16" FontWeight="Bold" Foreground="#1E40AF"/>
                                        </StackPanel>

                                        <TextBlock Text="يمكنك حفظ نسخة احتياطية تلقائية على مجلد مشترك في السيرفر" FontSize="12" Foreground="#6B7280" Margin="0,0,0,15"/>

                                        <CheckBox x:Name="ServerBackupCheckBox" Content="تفعيل النسخ الاحتياطي على السيرفر" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,15"/>

                                        <TextBlock Text="مسار المجلد المشترك (مثال: \\SERVER\ZKTecoBackups):" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                        <Grid Margin="0,0,0,15">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBox x:Name="ServerBackupPathTextBox" Grid.Column="0" Style="{StaticResource ModernTextBox}" Margin="0,0,10,0"
                                                     Text="" FlowDirection="LeftToRight" HorizontalContentAlignment="Left"/>
                                            <Button Grid.Column="1" Content="📁 استعراض" Style="{StaticResource PrimaryButton}" Width="120" Click="BrowseServerPath_Click"/>
                                        </Grid>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="الفاصل الزمني بين النسخ (بالأيام):" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                                <TextBox x:Name="ServerBackupIntervalTextBox" Style="{StaticResource ModernTextBox}" Text="10" Width="120" HorizontalAlignment="Right"/>
                                            </StackPanel>
                                        </Grid>

                                        <TextBlock x:Name="LastServerBackupText" Text="آخر نسخة للسيرفر: لا توجد" FontSize="12" Foreground="#6B7280" Margin="0,15,0,0"/>

                                        <Button Content="🔗 اختبار الاتصال بالسيرفر" Style="{StaticResource PrimaryButton}" Margin="0,15,0,0" Click="TestServerConnection_Click"/>
                                    </StackPanel>
                                </Border>

                                <!-- Save Button -->
                                <Button Content="💾 حفظ الإعدادات" Style="{StaticResource SuccessButton}" Click="SaveSettings_Click"/>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- Reset Tab -->
                    <TabItem Header="اعادة تعيين" FontSize="14" FontWeight="SemiBold" Foreground="#DC2626">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="5">
                            <StackPanel Margin="15">
                                <Border Background="#FEF2F2" CornerRadius="12" Padding="20" BorderBrush="#FECACA" BorderThickness="2">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                            <TextBlock Text="⚠️" FontSize="28" Margin="0,0,15,0"/>
                                            <StackPanel>
                                                <TextBlock Text="اعادة تعيين قاعدة البيانات" FontSize="18" FontWeight="Bold" Foreground="#DC2626"/>
                                                <TextBlock Text="العمليات في هذا القسم لا يمكن التراجع عنها!" FontSize="13" Foreground="#991B1B" Margin="0,5,0,0"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Reset Database Section -->
                                        <Border Background="White" CornerRadius="10" Padding="20" Margin="0,10,0,0" BorderBrush="#FCA5A5" BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="إعادة تعيين قاعدة البيانات" FontSize="15" FontWeight="Bold" Foreground="#1F2937"/>
                                                    <TextBlock Text="سيتم حذف جميع البيانات (الموظفين، سجلات الحضور، الأقسام، الورديات، الاستثناءات) مع الإبقاء على هيكل الجداول."
                                                               FontSize="12" Foreground="#6B7280" TextWrapping="Wrap" Margin="0,8,0,0" MaxWidth="450"/>
                                                    <TextBlock Text="⚠️ هذا الإجراء لا يمكن التراجع عنه!" FontSize="12" FontWeight="Bold" Foreground="#DC2626" Margin="0,8,0,0"/>
                                                </StackPanel>

                                                <Button Grid.Column="1" Content="🗑️ إعادة التعيين" Style="{StaticResource DangerButton}"
                                                        Width="140" Click="ResetDatabase_Click" VerticalAlignment="Center" Margin="15,0,0,0"/>
                                            </Grid>
                                        </Border>

                                        <!-- Clear Attendance Logs Only -->
                                        <Border Background="White" CornerRadius="10" Padding="20" Margin="0,15,0,0" BorderBrush="#FCA5A5" BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="مسح سجلات الحضور فقط" FontSize="15" FontWeight="Bold" Foreground="#1F2937"/>
                                                    <TextBlock Text="سيتم حذف جميع سجلات الحضور مع الإبقاء على بيانات الموظفين والأقسام والورديات."
                                                               FontSize="12" Foreground="#6B7280" TextWrapping="Wrap" Margin="0,8,0,0" MaxWidth="450"/>
                                                </StackPanel>

                                                <Button Grid.Column="1" Content="🗑️ مسح السجلات" Style="{StaticResource DangerButton}"
                                                        Width="140" Click="ClearAttendanceLogs_Click" VerticalAlignment="Center" Margin="15,0,0,0"/>
                                            </Grid>
                                        </Border>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border x:Name="LoadingOverlay" Visibility="Collapsed" Background="#80000000" CornerRadius="15">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border Background="White" CornerRadius="15" Padding="40">
                    <StackPanel>
                        <!-- Spinning animation -->
                        <Border Width="60" Height="60" CornerRadius="30" BorderThickness="4" BorderBrush="#667EEA" Margin="0,0,0,20">
                            <Border.RenderTransform>
                                <RotateTransform x:Name="SpinnerRotate" CenterX="30" CenterY="30"/>
                            </Border.RenderTransform>
                            <Border.Triggers>
                                <EventTrigger RoutedEvent="Border.Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="SpinnerRotate"
                                                           Storyboard.TargetProperty="Angle"
                                                           From="0" To="360" Duration="0:0:1"
                                                           RepeatBehavior="Forever"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Border.Triggers>
                            <Border Width="50" Height="50" CornerRadius="25" Background="White"/>
                        </Border>
                        <TextBlock x:Name="LoadingText" Text="جاري الاستعادة..." FontSize="16" FontWeight="SemiBold"
                                   Foreground="#374151" HorizontalAlignment="Center"/>
                        <TextBlock Text="الرجاء الانتظار" FontSize="12" Foreground="#6B7280"
                                   HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</Window>
