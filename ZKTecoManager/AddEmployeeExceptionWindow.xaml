<Window x:Class="ZKTecoManager.AddEmployeeExceptionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إضافة استثناء موظف" Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft">

    <Border CornerRadius="12" Background="White">
        <Border.Effect>
            <DropShadowEffect Color="#1F2937" Opacity="0.2" ShadowDepth="5" BlurRadius="20"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="55"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" CornerRadius="12,12,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#667EEA" Offset="0"/>
                        <GradientStop Color="#764BA2" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <TextBlock x:Name="TitleText" Text="إضافة استثناء جديد"
                               FontSize="18" FontWeight="Bold" Foreground="White"
                               VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <Button Content="✕" Click="CloseButton_Click"
                            HorizontalAlignment="Left" VerticalAlignment="Center"
                            Margin="15,0" Width="30" Height="30"
                            Background="Transparent" Foreground="White"
                            BorderThickness="0" Cursor="Hand" FontSize="14"/>
                </Grid>
            </Border>

            <!-- Form -->
            <StackPanel Grid.Row="1" Margin="25,20">
                <!-- Employee -->
                <TextBlock Text="الموظف:" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>
                <ComboBox x:Name="EmployeeComboBox" Height="40" FontSize="14"
                          DisplayMemberPath="DisplayText" SelectedValuePath="UserId"
                          Margin="0,0,0,15"/>

                <!-- Exception Type -->
                <TextBlock Text="نوع الاستثناء:" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>
                <ComboBox x:Name="ExceptionTypeComboBox" Height="40" FontSize="14"
                          DisplayMemberPath="ExceptionName" SelectedValuePath="ExceptionTypeId"
                          Margin="0,0,0,15"/>

                <!-- Date -->
                <TextBlock Text="التاريخ:" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>
                <DatePicker x:Name="ExceptionDatePicker" Height="40" FontSize="14" Margin="0,0,0,15"/>

                <!-- Notes -->
                <TextBlock Text="ملاحظات (اختياري):" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>
                <TextBox x:Name="NotesTextBox" Height="50" FontSize="14" TextWrapping="Wrap"
                         AcceptsReturn="True" VerticalContentAlignment="Top" Padding="8"/>
            </StackPanel>

            <!-- Buttons -->
            <Border Grid.Row="2" Background="#F9FAFB" CornerRadius="0,0,12,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button x:Name="SaveButton" Content="حفظ" Click="SaveButton_Click"
                            Width="120" Height="38" FontSize="14" FontWeight="SemiBold"
                            Background="#10B981" Foreground="White" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button Content="إلغاء" Click="CancelButton_Click"
                            Width="120" Height="38" FontSize="14" FontWeight="SemiBold"
                            Background="#6B7280" Foreground="White" BorderThickness="0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
