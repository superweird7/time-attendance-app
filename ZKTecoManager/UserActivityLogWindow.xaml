<Window x:Class="ZKTecoManager.UserActivityLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="سجل نشاط المستخدمين - User Activity Log" Height="700" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResize"
        MinWidth="800"
        MinHeight="500"
        FlowDirection="RightToLeft">

    <Window.Resources>
        <!-- Action Button Style (Blue) -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Segoe UI, Tahoma, Arial"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#3B82F6" Offset="0"/>
                                    <GradientStop Color="#2563EB" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#2563EB" Offset="0"/>
                                            <GradientStop Color="#1D4ED8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Export Button Style (Green) -->
        <Style x:Key="ExportButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Segoe UI, Tahoma, Arial"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#10B981" Offset="0"/>
                                    <GradientStop Color="#059669" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#059669" Offset="0"/>
                                            <GradientStop Color="#047857" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Window Control Button -->
        <Style x:Key="WinBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#40FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontFamily" Value="Segoe UI, Tahoma, Arial"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="White">
        <Border.Effect>
            <DropShadowEffect Color="#1F2937" Opacity="0.2" ShadowDepth="5" BlurRadius="20"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" CornerRadius="15,15,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#6366F1" Offset="0"/>
                        <GradientStop Color="#8B5CF6" Offset="0.5"/>
                        <GradientStop Color="#A855F7" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Grid>
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Right"
                                Margin="25,0"
                                FlowDirection="LeftToRight">
                        <TextBlock Text="📋" FontSize="22" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <TextBlock Text="سجل نشاط المستخدمين"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="White"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Margin="15,0"
                                FlowDirection="LeftToRight">
                        <Button Content="─" Style="{StaticResource WinBtn}" Click="MinimizeButton_Click" Margin="5,0"/>
                        <Button Content="✕" Style="{StaticResource WinBtn}" Click="CloseButton_Click" Margin="5,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Filters -->
            <Border Grid.Row="1" Background="#F9FAFB" Padding="20,15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- User Filter -->
                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="المستخدم" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                        <ComboBox x:Name="UserFilterComboBox"
                                  Style="{StaticResource ModernComboBox}"
                                  SelectionChanged="Filter_Changed"/>
                    </StackPanel>

                    <!-- Action Type Filter -->
                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="10,0">
                        <TextBlock Text="نوع النشاط" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                        <ComboBox x:Name="ActionTypeComboBox"
                                  Style="{StaticResource ModernComboBox}"
                                  SelectionChanged="Filter_Changed">
                            <ComboBoxItem Content="الكل" IsSelected="True"/>
                            <ComboBoxItem Content="تسجيل الدخول"/>
                            <ComboBoxItem Content="تسجيل الخروج"/>
                            <ComboBoxItem Content="تغيير كلمة المرور"/>
                            <ComboBoxItem Content="إنشاء مستخدم"/>
                            <ComboBoxItem Content="تعديل مستخدم"/>
                            <ComboBoxItem Content="حذف مستخدم"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- From Date -->
                    <StackPanel Grid.Row="0" Grid.Column="2" Margin="10,0">
                        <TextBlock Text="من تاريخ" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                        <DatePicker x:Name="FromDatePicker"
                                    Height="40"
                                    FontSize="13"
                                    SelectedDateChanged="Filter_Changed"/>
                    </StackPanel>

                    <!-- To Date -->
                    <StackPanel Grid.Row="0" Grid.Column="3" Margin="10,0">
                        <TextBlock Text="إلى تاريخ" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                        <DatePicker x:Name="ToDatePicker"
                                    Height="40"
                                    FontSize="13"
                                    SelectedDateChanged="Filter_Changed"/>
                    </StackPanel>

                    <!-- Employee Search (Row 1, Column 0-1) -->
                    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,15,10,0">
                        <TextBlock Text="بحث عن موظف (الاسم أو الوصف)" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                        <TextBox x:Name="EmployeeSearchTextBox"
                                 Height="40"
                                 Padding="12"
                                 FontSize="13"
                                 BorderBrush="#E5E7EB"
                                 BorderThickness="2"
                                 TextChanged="EmployeeSearchTextBox_TextChanged"/>
                    </StackPanel>

                    <!-- Table Filter (Row 1, Column 2) -->
                    <StackPanel Grid.Row="1" Grid.Column="2" Margin="10,15,10,0">
                        <TextBlock Text="الجدول" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                        <ComboBox x:Name="TableFilterComboBox"
                                  Style="{StaticResource ModernComboBox}"
                                  SelectionChanged="TableFilterComboBox_SelectionChanged">
                            <ComboBoxItem Content="الكل" IsSelected="True" Tag=""/>
                            <ComboBoxItem Content="الموظفين" Tag="users"/>
                            <ComboBoxItem Content="سجلات الحضور" Tag="attendance_logs"/>
                            <ComboBoxItem Content="الأقسام" Tag="departments"/>
                            <ComboBoxItem Content="الدوامات" Tag="shifts"/>
                            <ComboBoxItem Content="الأجهزة" Tag="machines"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Buttons (Row 1, Column 3) -->
                    <StackPanel Grid.Row="1" Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="10,15,0,0">
                        <Button Content="تحديث"
                                Style="{StaticResource ActionButton}"
                                Click="RefreshButton_Click"
                                Width="80"
                                Margin="0,0,10,0"/>
                        <Button Content="تصدير"
                                Style="{StaticResource ExportButton}"
                                Click="ExportButton_Click"
                                Width="80"
                                Margin="0,0,10,0"/>
                        <Button Content="مسح الكل"
                                Click="ClearLogsButton_Click"
                                Width="90"
                                Height="40"
                                FontSize="13"
                                FontWeight="Bold"
                                Foreground="White"
                                Cursor="Hand"
                                BorderThickness="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" CornerRadius="8">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                <GradientStop Color="#EF4444" Offset="0"/>
                                                <GradientStop Color="#DC2626" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background">
                                                <Setter.Value>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                        <GradientStop Color="#DC2626" Offset="0"/>
                                                        <GradientStop Color="#B91C1C" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content Area (DataGrid + Detail Panel) -->
            <Grid Grid.Row="2" Margin="20,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Activity Log Grid -->
                <DataGrid Grid.Row="0"
                          x:Name="ActivityLogDataGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          Background="White"
                          BorderBrush="#E5E7EB"
                          BorderThickness="1"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#F3F4F6"
                          RowHeight="45"
                          ColumnHeaderHeight="45"
                          FontSize="13"
                          FontFamily="Segoe UI, Tahoma, Arial"
                          SelectionChanged="ActivityLogDataGrid_SelectionChanged">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F9FAFB"/>
                        <Setter Property="Foreground" Value="#374151"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="15,10"/>
                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        <Setter Property="BorderBrush" Value="#E5E7EB"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#EEF2FF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F9FAFB"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="15,10"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#1F2937"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="التاريخ والوقت" Binding="{Binding CreatedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" Width="180"/>
                    <DataGridTextColumn Header="المستخدم" Binding="{Binding UserName}" Width="150"/>
                    <DataGridTemplateColumn Header="نوع النشاط" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Padding="8,4" CornerRadius="4" HorizontalAlignment="Right">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#E5E7EB"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ActionType}" Value="LOGIN">
                                                    <Setter Property="Background" Value="#D1FAE5"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ActionType}" Value="LOGOUT">
                                                    <Setter Property="Background" Value="#FEE2E2"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ActionType}" Value="PASSWORD_CHANGE">
                                                    <Setter Property="Background" Value="#FEF3C7"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ActionType}" Value="CREATE">
                                                    <Setter Property="Background" Value="#DBEAFE"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ActionType}" Value="UPDATE">
                                                    <Setter Property="Background" Value="#E0E7FF"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ActionType}" Value="DELETE">
                                                    <Setter Property="Background" Value="#FCE7F3"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding ActionTypeDisplay}" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#374151"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ActionType}" Value="LOGIN">
                                                        <Setter Property="Foreground" Value="#059669"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ActionType}" Value="LOGOUT">
                                                        <Setter Property="Foreground" Value="#DC2626"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ActionType}" Value="PASSWORD_CHANGE">
                                                        <Setter Property="Foreground" Value="#D97706"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ActionType}" Value="CREATE">
                                                        <Setter Property="Foreground" Value="#2563EB"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ActionType}" Value="UPDATE">
                                                        <Setter Property="Foreground" Value="#4F46E5"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ActionType}" Value="DELETE">
                                                        <Setter Property="Foreground" Value="#DB2777"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="الجدول" Binding="{Binding TableName}" Width="120"/>
                    <DataGridTextColumn Header="الوصف" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="IP" Binding="{Binding IpAddress}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>

                <!-- Before/After Comparison Panel -->
                <Expander Grid.Row="1"
                          x:Name="DetailExpander"
                          Header="مقارنة القيم - Before/After Comparison"
                          Margin="0,15,0,0"
                          IsExpanded="False"
                          BorderBrush="#E5E7EB"
                          BorderThickness="1"
                          Background="White">
                    <Border Background="#F9FAFB" Padding="20" Margin="0,10,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                        <!-- Before Value -->
                        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="15" Margin="0,0,10,0" BorderBrush="#E5E7EB" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="قبل - Before" FontWeight="Bold" FontSize="14" Margin="0,0,0,10" Foreground="#374151"/>
                                <TextBox x:Name="OldValueTextBox"
                                         IsReadOnly="True"
                                         TextWrapping="Wrap"
                                         MinHeight="80"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         FontSize="13"
                                         Foreground="#6B7280"/>
                            </StackPanel>
                        </Border>

                        <!-- After Value -->
                        <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="15" Margin="10,0,0,0" BorderBrush="#E5E7EB" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="بعد - After" FontWeight="Bold" FontSize="14" Margin="0,0,0,10" Foreground="#374151"/>
                                <TextBox x:Name="NewValueTextBox"
                                         IsReadOnly="True"
                                         TextWrapping="Wrap"
                                         MinHeight="80"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         FontSize="13"
                                         Foreground="#6B7280"/>
                            </StackPanel>
                        </Border>
                        </Grid>
                    </Border>
                </Expander>
            </Grid>

            <!-- Footer -->
            <Border Grid.Row="3" Background="#F9FAFB" Padding="20,12">
                <Grid>
                    <TextBlock x:Name="RecordCountText"
                               Text="إجمالي السجلات: 0"
                               FontSize="13"
                               Foreground="#6B7280"
                               HorizontalAlignment="Right"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
