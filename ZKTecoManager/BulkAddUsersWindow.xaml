<Window x:Class="ZKTecoManager.BulkAddUsersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إضافة مستخدمين - Add Users" Height="600" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft">

    <Border CornerRadius="15" Background="White">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.3" ShadowDepth="5" BlurRadius="20"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" CornerRadius="15,15,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#667EEA" Offset="0"/>
                        <GradientStop Color="#764BA2" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="25,0">
                        <TextBlock Text="➕" FontSize="24" Margin="0,0,10,0"/>
                        <TextBlock Text="إضافة مستخدمين جدد" FontSize="20" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>

                    <Button Content="✕" Width="40" Height="40" Background="Transparent" Foreground="White" 
                            BorderThickness="0" FontSize="18" FontWeight="Bold" Cursor="Hand"
                            HorizontalAlignment="Left" Margin="15,0" Click="CancelButton_Click"/>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="30">
                    <!-- Info -->
                    <Border Background="#E3F2FD" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                        <TextBlock Text="قم بإدخال اسم وقسم لكل مستخدم جديد" FontSize="14" Foreground="#1565C0"/>
                    </Border>

                    <!-- Department Selection -->
                    <TextBlock Text="القسم الافتراضي للجميع" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <ComboBox x:Name="DefaultDepartmentComboBox" Height="45" FontSize="14" Margin="0,0,0,20"/>

                    <!-- Users List -->
                    <ItemsControl x:Name="UsersItemsControl">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F9FAFB" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="{Binding BadgeNumber}" FontSize="16" 
                                                   FontWeight="Bold" Foreground="#667EEA" Width="120" VerticalAlignment="Center"/>

                                        <TextBox Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                                                 Height="40" FontSize="14" Padding="10"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>

            <!-- Buttons -->
            <Grid Grid.Row="2" Margin="30,0,30,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="إلغاء" Height="50" FontSize="15" Background="#6B7280" 
                        Foreground="White" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0" 
                        Click="CancelButton_Click"/>

                <Button Grid.Column="1" Content="✅ إضافة الكل" Height="50" FontSize="15" Background="#10B981" 
                        Foreground="White" BorderThickness="0" Cursor="Hand" FontWeight="Bold" Margin="10,0,0,0"
                        Click="SaveButton_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
