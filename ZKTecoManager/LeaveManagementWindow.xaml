<Window x:Class="ZKTecoManager.LeaveManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إدارة الإجازات - Leave Management" Height="700" Width="1100"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="CanResize"
        MinWidth="900"
        MinHeight="600"
        FlowDirection="RightToLeft"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Card Style -->
        <Style x:Key="BalanceCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.1" ShadowDepth="2" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button Style -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="#F3F4F6">
        <Border.Effect>
            <DropShadowEffect Color="#1F2937" Opacity="0.2" ShadowDepth="5" BlurRadius="20"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" CornerRadius="15,15,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#10B981" Offset="0"/>
                        <GradientStop Color="#059669" Offset="0.5"/>
                        <GradientStop Color="#047857" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Grid>
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Right"
                                Margin="25,0"
                                FlowDirection="LeftToRight">
                        <TextBlock Text="📋" FontSize="24" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <TextBlock Text="إدارة الإجازات - Leave Management"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="White"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="15,0">
                        <Button Content="✕" Click="CloseButton_Click"
                                Width="35" Height="35"
                                Background="Transparent" Foreground="White"
                                BorderThickness="0" Cursor="Hand" FontSize="16"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Employee Selection & Action Buttons -->
            <Border Grid.Row="1" Background="White" Padding="20,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Department & Employee Selection -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <!-- Department Selection -->
                        <TextBlock Text="القسم:" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Margin="0,0,10,0"/>
                        <ComboBox x:Name="DepartmentComboBox"
                                  Width="200" Height="38"
                                  FontSize="14"
                                  DisplayMemberPath="DeptName"
                                  SelectionChanged="DepartmentComboBox_SelectionChanged"/>

                        <!-- Employee Selection (dependent on department) -->
                        <TextBlock Text="الموظف:" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Margin="20,0,10,0"/>
                        <ComboBox x:Name="EmployeeComboBox"
                                  Width="280" Height="38"
                                  FontSize="14"
                                  IsEditable="True"
                                  IsEnabled="False"
                                  SelectionChanged="EmployeeComboBox_SelectionChanged">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding BadgeNumber}" FontWeight="Bold" Margin="0,0,10,0"/>
                                        <TextBlock Text="-"/>
                                        <TextBlock Text="{Binding Name}" Margin="10,0,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <Button Content="تحديث" Click="RefreshButton_Click"
                                Margin="10,0,0,0" Height="38" Padding="15,0"
                                Background="#6B7280" Style="{StaticResource ActionButton}"/>
                    </StackPanel>

                    <!-- Action Buttons -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" FlowDirection="LeftToRight">
                        <Button Click="AddLeaveButton_Click"
                                Background="#10B981" Style="{StaticResource ActionButton}" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="➕" Margin="0,0,8,0"/>
                                <TextBlock Text="تسجيل إجازة"/>
                            </StackPanel>
                        </Button>
                        <Button Click="AddHourlyLeaveButton_Click"
                                Background="#3B82F6" Style="{StaticResource ActionButton}" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⏰" Margin="0,0,8,0"/>
                                <TextBlock Text="إجازة ساعية"/>
                            </StackPanel>
                        </Button>
                        <Button Click="ViewBalanceButton_Click"
                                Background="#8B5CF6" Style="{StaticResource ActionButton}" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📊" Margin="0,0,8,0"/>
                                <TextBlock Text="أرصدة الموظف"/>
                            </StackPanel>
                        </Button>
                        <Button Click="ReportsButton_Click"
                                Background="#F59E0B" Style="{StaticResource ActionButton}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📄" Margin="0,0,8,0"/>
                                <TextBlock Text="التقارير"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Balance Summary Cards -->
            <Border Grid.Row="2" Background="#F9FAFB" Padding="15,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Ordinary Leave Card -->
                    <Border Grid.Column="0" Style="{StaticResource BalanceCard}">
                        <StackPanel>
                            <TextBlock Text="الاجازة الاعتيادية" FontSize="12" Foreground="#6B7280" TextAlignment="Center"/>
                            <TextBlock Text="Ordinary Leave" FontSize="10" Foreground="#9CA3AF" TextAlignment="Center" Margin="0,2,0,8"/>
                            <TextBlock x:Name="OrdinaryBalanceText" Text="--" FontSize="28" FontWeight="Bold" Foreground="#10B981" TextAlignment="Center"/>
                            <TextBlock Text="يوم متبقي" FontSize="11" Foreground="#6B7280" TextAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Sick Full Pay Card -->
                    <Border Grid.Column="1" Style="{StaticResource BalanceCard}">
                        <StackPanel>
                            <TextBlock Text="مرضية براتب كامل" FontSize="12" Foreground="#6B7280" TextAlignment="Center"/>
                            <TextBlock Text="Sick (Full Pay)" FontSize="10" Foreground="#9CA3AF" TextAlignment="Center" Margin="0,2,0,8"/>
                            <TextBlock x:Name="SickFullBalanceText" Text="--" FontSize="28" FontWeight="Bold" Foreground="#3B82F6" TextAlignment="Center"/>
                            <TextBlock Text="يوم متبقي" FontSize="11" Foreground="#6B7280" TextAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Sick Half Pay Card -->
                    <Border Grid.Column="2" Style="{StaticResource BalanceCard}">
                        <StackPanel>
                            <TextBlock Text="مرضية بنصف راتب" FontSize="12" Foreground="#6B7280" TextAlignment="Center"/>
                            <TextBlock Text="Sick (Half Pay)" FontSize="10" Foreground="#9CA3AF" TextAlignment="Center" Margin="0,2,0,8"/>
                            <TextBlock x:Name="SickHalfBalanceText" Text="--" FontSize="28" FontWeight="Bold" Foreground="#8B5CF6" TextAlignment="Center"/>
                            <TextBlock Text="يوم متبقي" FontSize="11" Foreground="#6B7280" TextAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Unpaid Leave Card -->
                    <Border Grid.Column="3" Style="{StaticResource BalanceCard}">
                        <StackPanel>
                            <TextBlock Text="إجازة بدون راتب" FontSize="12" Foreground="#6B7280" TextAlignment="Center"/>
                            <TextBlock Text="Unpaid Leave" FontSize="10" Foreground="#9CA3AF" TextAlignment="Center" Margin="0,2,0,8"/>
                            <TextBlock x:Name="UnpaidUsedText" Text="--" FontSize="28" FontWeight="Bold" Foreground="#EF4444" TextAlignment="Center"/>
                            <TextBlock Text="يوم هذا الشهر" FontSize="11" Foreground="#6B7280" TextAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Hourly Accumulator Card -->
                    <Border Grid.Column="4" Style="{StaticResource BalanceCard}">
                        <StackPanel>
                            <TextBlock Text="الساعات المتراكمة" FontSize="12" Foreground="#6B7280" TextAlignment="Center"/>
                            <TextBlock Text="Accumulated Hours" FontSize="10" Foreground="#9CA3AF" TextAlignment="Center" Margin="0,2,0,8"/>
                            <TextBlock x:Name="HourlyAccumulatorText" Text="--" FontSize="28" FontWeight="Bold" Foreground="#F59E0B" TextAlignment="Center"/>
                            <TextBlock Text="ساعة (7 = يوم)" FontSize="11" Foreground="#6B7280" TextAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Recent Transactions Grid -->
            <Border Grid.Row="3" Background="White" Margin="15" CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Opacity="0.05" ShadowDepth="2" BlurRadius="8"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Section Header -->
                    <Border Grid.Row="0" Background="#F9FAFB" CornerRadius="12,12,0,0" Padding="15,12">
                        <Grid>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBlock Text="📜" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="آخر العمليات - Recent Transactions" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" FlowDirection="LeftToRight">
                                <TextBlock Text="السنة:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <ComboBox x:Name="YearComboBox" Width="100" Height="30" SelectionChanged="YearComboBox_SelectionChanged"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- DataGrid -->
                    <DataGrid x:Name="TransactionsDataGrid"
                              Grid.Row="1"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              BorderThickness="0"
                              Background="White"
                              RowBackground="White"
                              AlternatingRowBackground="#F9FAFB"
                              FontSize="13"
                              SelectionMode="Single">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F3F4F6"/>
                                <Setter Property="Foreground" Value="#374151"/>
                                <Setter Property="Padding" Value="12,10"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#E5E7EB"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="12,8"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#DBEAFE"/>
                                        <Setter Property="Foreground" Value="#1F2937"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="التاريخ" Binding="{Binding SubmissionDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                            <DataGridTextColumn Header="رقم الموظف" Binding="{Binding UserBadgeNumber}" Width="100"/>
                            <DataGridTextColumn Header="اسم الموظف" Binding="{Binding UserName}" Width="150"/>
                            <DataGridTextColumn Header="نوع الإجازة" Binding="{Binding LeaveTypeName}" Width="140"/>
                            <DataGridTextColumn Header="نوع العملية" Binding="{Binding TransactionTypeDisplay}" Width="100"/>
                            <DataGridTextColumn Header="الأيام" Binding="{Binding DaysAmount, StringFormat=N2}" Width="70"/>
                            <DataGridTextColumn Header="الساعات" Binding="{Binding HoursAmount, StringFormat=N2}" Width="70"/>
                            <DataGridTextColumn Header="من" Binding="{Binding StartDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                            <DataGridTextColumn Header="إلى" Binding="{Binding EndDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                            <DataGridTextColumn Header="السبب" Binding="{Binding Reason}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Loading Overlay -->
            <Border x:Name="LoadingOverlay" Grid.RowSpan="4" Background="#80FFFFFF" Visibility="Collapsed">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="جاري التحميل..." FontSize="18" Foreground="#374151"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
