<Window x:Class="ZKTecoManager.SyncHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="سجل المزامنة - Sync History" Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="White"
        WindowStyle="None"
        AllowsTransparency="True"
        FlowDirection="RightToLeft">

    <Window.Resources>
        <Style x:Key="WinBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#40FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="White">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.3" ShadowDepth="5" BlurRadius="20"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" CornerRadius="15,15,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#667EEA" Offset="0"/>
                        <GradientStop Color="#764BA2" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="20,0">
                        <TextBlock Text="📊" FontSize="20" Margin="0,0,10,0"/>
                        <TextBlock Text="سجل المزامنة" FontSize="18" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>

                    <Button Content="✕" Style="{StaticResource WinBtn}" Click="CloseButton_Click" HorizontalAlignment="Left" Margin="15,0"/>
                </Grid>
            </Border>

            <!-- Stats Summary -->
            <Border Grid.Row="1" Background="#F0F9FF" Margin="20,15,20,0" CornerRadius="10" Padding="20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                        <TextBlock x:Name="TotalSyncsText" Text="0" FontSize="28" FontWeight="Bold" Foreground="#667EEA" HorizontalAlignment="Center"/>
                        <TextBlock Text="إجمالي المزامنات" FontSize="12" Foreground="#6B7280" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                        <TextBlock x:Name="SuccessfulSyncsText" Text="0" FontSize="28" FontWeight="Bold" Foreground="#10B981" HorizontalAlignment="Center"/>
                        <TextBlock Text="ناجحة" FontSize="12" Foreground="#6B7280" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                        <TextBlock x:Name="FailedSyncsText" Text="0" FontSize="28" FontWeight="Bold" Foreground="#EF4444" HorizontalAlignment="Center"/>
                        <TextBlock Text="فاشلة" FontSize="12" Foreground="#6B7280" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                        <TextBlock x:Name="TotalRecordsText" Text="0" FontSize="28" FontWeight="Bold" Foreground="#F59E0B" HorizontalAlignment="Center"/>
                        <TextBlock Text="سجلات مُزامَنة" FontSize="12" Foreground="#6B7280" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- History Grid -->
            <Border Grid.Row="2" Margin="20" Background="#F9FAFB" CornerRadius="10" Padding="10">
                <DataGrid x:Name="HistoryGrid" AutoGenerateColumns="False" IsReadOnly="True"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                          BorderThickness="0" Background="Transparent"
                          RowHeight="45" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="الموقع" Binding="{Binding LocationName}" Width="120"/>
                        <DataGridTextColumn Header="التاريخ" Binding="{Binding CompletedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="130"/>
                        <DataGridTemplateColumn Header="الحالة" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="10" Padding="8,4" HorizontalAlignment="Center"
                                            Background="{Binding StatusColor}">
                                        <TextBlock Text="{Binding StatusDisplay}" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="إضافة" Binding="{Binding RecordsAdded}" Width="70"/>
                        <DataGridTextColumn Header="تحديث" Binding="{Binding RecordsUpdated}" Width="70"/>
                        <DataGridTextColumn Header="تخطي" Binding="{Binding RecordsSkipped}" Width="70"/>
                        <DataGridTextColumn Header="المدة" Binding="{Binding DurationDisplay}" Width="80"/>
                        <DataGridTemplateColumn Header="النوع" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding SyncTypeDisplay}" FontSize="11" Foreground="#6B7280"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="رسالة الخطأ" Binding="{Binding ErrorMessage}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Border>
</Window>
