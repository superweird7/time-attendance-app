<Window x:Class="ZKTecoManager.EditExceptionTypeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تعديل استثناء" Height="180" Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft">

    <Border CornerRadius="10" Background="White" BorderBrush="#667EEA" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="45"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="55"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#667EEA" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <TextBlock Text="تعديل الاستثناء" FontSize="16" FontWeight="Bold"
                           Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="20,15">
                <TextBlock Text="اسم الاستثناء:" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>
                <TextBox x:Name="NameTextBox" Height="35" FontSize="14" Padding="10,5"
                         VerticalContentAlignment="Center"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Content="حفظ" Click="SaveButton_Click" Width="100" Height="35"
                        Background="#10B981" Foreground="White" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Content="إلغاء" Click="CancelButton_Click" Width="100" Height="35"
                        Background="#6B7280" Foreground="White" BorderThickness="0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
