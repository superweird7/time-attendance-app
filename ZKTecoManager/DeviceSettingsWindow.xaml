<Window x:Class="ZKTecoManager.DeviceSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إعدادات الجهاز - Device Settings" Height="750" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft">

    <Window.Resources>
        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI, Tahoma, Arial"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="Center"
                                          Margin="5,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#667EEA"/>
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#667EEA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI, Tahoma, Arial"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#667EEA" Offset="0"/>
                                    <GradientStop Color="#764BA2" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="#667EEA" Opacity="0.4" ShadowDepth="0" BlurRadius="12"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#5568D3" Offset="0"/>
                                            <GradientStop Color="#6A3E8F" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#9CA3AF"/>
                                <Setter TargetName="border" Property="Effect" Value="{x:Null}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Success Button (Green) -->
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#10B981" Offset="0"/>
                                    <GradientStop Color="#059669" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="#10B981" Opacity="0.3" ShadowDepth="0" BlurRadius="10"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#0EA06E" Offset="0"/>
                                            <GradientStop Color="#047857" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Cancel Button -->
        <Style x:Key="CancelButton" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI, Tahoma, Arial"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F3F4F6"/>
                                <Setter Property="Foreground" Value="#374151"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Window Control Button -->
        <Style x:Key="WinBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#40FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Toggle Switch Style -->
        <Style x:Key="ToggleSwitch" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Border x:Name="track" Width="50" Height="28" CornerRadius="14"
                                    Background="#E5E7EB" Cursor="Hand">
                                <Border x:Name="thumb" Width="22" Height="22" CornerRadius="11"
                                        Background="White" HorizontalAlignment="Left" Margin="3,0,0,0">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="1" Opacity="0.3" BlurRadius="3"/>
                                    </Border.Effect>
                                </Border>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="track" Property="Background" Value="#10B981"/>
                                <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="thumb" Property="Margin" Value="0,0,3,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="White">
        <Border.Effect>
            <DropShadowEffect Color="#1F2937" Opacity="0.3" ShadowDepth="5" BlurRadius="25"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Gradient Header -->
            <Border Grid.Row="0" CornerRadius="15,15,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#667EEA" Offset="0"/>
                        <GradientStop Color="#764BA2" Offset="0.5"/>
                        <GradientStop Color="#F093FB" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Grid>
                    <!-- Title -->
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Right"
                                Margin="25,0"
                                FlowDirection="LeftToRight">
                        <TextBlock Text="⚙️" FontSize="24" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <TextBlock x:Name="TitleText" Text="إعدادات الجهاز"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="White"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Close Button -->
                    <Button Style="{StaticResource WinBtn}"
                            Content="✕"
                            Click="CloseButton_Click"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Margin="15,0"
                            FlowDirection="LeftToRight"/>
                </Grid>
            </Border>

            <!-- Main Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="30,25,30,30">
                    <!-- Device Info -->
                    <Border Background="#F0F4FF" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="معلومات الجهاز" FontSize="14" FontWeight="SemiBold" Foreground="#667EEA" Margin="0,0,0,10"/>
                            <TextBlock x:Name="DeviceInfoText" Text="الجهاز: ..." FontSize="14" Foreground="#374151"/>
                        </StackPanel>
                    </Border>

                    <!-- Device Statistics Section -->
                    <Border Background="#ECFDF5" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="📊 إحصائيات الجهاز" FontSize="16" FontWeight="Bold" Foreground="#047857" Margin="0,0,0,15"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Users Count -->
                                <Border Grid.Column="0" Grid.Row="0" Background="White" CornerRadius="8" Padding="12" Margin="0,0,5,10">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="👥" FontSize="20" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="UserCountText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#10B981" HorizontalAlignment="Center"/>
                                        <TextBlock Text="المستخدمين" FontSize="11" Foreground="#6B7280" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <!-- Fingerprints Count -->
                                <Border Grid.Column="1" Grid.Row="0" Background="White" CornerRadius="8" Padding="12" Margin="5,0,0,10">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="👆" FontSize="20" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="FingerprintCountText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#667EEA" HorizontalAlignment="Center"/>
                                        <TextBlock Text="البصمات" FontSize="11" Foreground="#6B7280" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <!-- Face Templates Count -->
                                <Border Grid.Column="0" Grid.Row="1" Background="White" CornerRadius="8" Padding="12" Margin="0,0,5,0">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="😀" FontSize="20" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="FaceCountText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#F59E0B" HorizontalAlignment="Center"/>
                                        <TextBlock Text="الوجوه" FontSize="11" Foreground="#6B7280" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <!-- Attendance Records Count -->
                                <Border Grid.Column="1" Grid.Row="1" Background="White" CornerRadius="8" Padding="12" Margin="5,0,0,0">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="📝" FontSize="20" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="RecordCountText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#EF4444" HorizontalAlignment="Center"/>
                                        <TextBlock Text="السجلات" FontSize="11" Foreground="#6B7280" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <Button Content="🔄 تحديث الإحصائيات" Style="{StaticResource PrimaryButton}"
                                    Height="35" Margin="0,15,0,0" Click="RefreshStats_Click" FontSize="12"/>
                        </StackPanel>
                    </Border>

                    <!-- Time Settings Section -->
                    <Border Background="#F9FAFB" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="⏰ إعدادات الوقت" FontSize="16" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="الوقت الحالي للجهاز" FontSize="14" Foreground="#374151"/>
                                    <TextBlock x:Name="DeviceTimeText" Text="--:--:--" FontSize="18" FontWeight="Bold" Foreground="#667EEA" Margin="0,5,0,0"/>
                                </StackPanel>
                                <Button Grid.Column="1" Content="🔄 قراءة الوقت" Style="{StaticResource PrimaryButton}"
                                        Width="130" Height="40" Click="ReadDeviceTime_Click"/>
                            </Grid>

                            <Button Content="🔄 مزامنة مع وقت الكمبيوتر" Style="{StaticResource SuccessButton}"
                                    Height="40" Margin="0,10,0,0" Click="SyncTime_Click"/>

                            <!-- Manual Time Edit Section -->
                            <Border Background="#FFF7ED" CornerRadius="8" Padding="15" Margin="0,15,0,0">
                                <StackPanel>
                                    <TextBlock Text="✏️ تعديل الوقت يدوياً" FontSize="13" FontWeight="SemiBold" Foreground="#C2410C" Margin="0,0,0,10"/>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="15"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Date Picker -->
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="التاريخ" FontSize="12" Foreground="#6B7280" Margin="0,0,0,5"/>
                                            <DatePicker x:Name="ManualDatePicker" FontSize="13" Height="38"/>
                                        </StackPanel>

                                        <!-- Time Picker (using TextBoxes) -->
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="الوقت (ساعة:دقيقة:ثانية)" FontSize="12" Foreground="#6B7280" Margin="0,0,0,5"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBox x:Name="ManualHourBox" Grid.Column="0" Text="12" TextAlignment="Center"
                                                         Style="{StaticResource ModernTextBox}" Height="38" FontSize="14" MaxLength="2"/>
                                                <TextBlock Grid.Column="1" Text=":" VerticalAlignment="Center" FontSize="18" FontWeight="Bold" Margin="3,0"/>
                                                <TextBox x:Name="ManualMinuteBox" Grid.Column="2" Text="00" TextAlignment="Center"
                                                         Style="{StaticResource ModernTextBox}" Height="38" FontSize="14" MaxLength="2"/>
                                                <TextBlock Grid.Column="3" Text=":" VerticalAlignment="Center" FontSize="18" FontWeight="Bold" Margin="3,0"/>
                                                <TextBox x:Name="ManualSecondBox" Grid.Column="4" Text="00" TextAlignment="Center"
                                                         Style="{StaticResource ModernTextBox}" Height="38" FontSize="14" MaxLength="2"/>
                                            </Grid>
                                        </StackPanel>
                                    </Grid>

                                    <Button Content="⏱️ تطبيق الوقت المحدد" Style="{StaticResource PrimaryButton}"
                                            Height="38" Margin="0,12,0,0" Click="ApplyManualTime_Click" FontSize="13"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- Sound Settings Section -->
                    <Border Background="#F9FAFB" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="🔊 إعدادات الصوت" FontSize="16" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>

                            <!-- Voice Mode -->
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="الصوت عند البصمة" FontSize="14" Foreground="#374151"/>
                                    <TextBlock Text="تشغيل صوت عند التعرف على البصمة" FontSize="12" Foreground="#9CA3AF"/>
                                </StackPanel>
                                <CheckBox x:Name="VoiceToggle" Grid.Column="1" Style="{StaticResource ToggleSwitch}" VerticalAlignment="Center"/>
                            </Grid>

                            <!-- Volume -->
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="مستوى الصوت" FontSize="14" Foreground="#374151" Margin="0,0,0,8"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="50"/>
                                    </Grid.ColumnDefinitions>
                                    <Slider x:Name="VolumeSlider" Grid.Column="0" Minimum="0" Maximum="100" Value="50"
                                            VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding ElementName=VolumeSlider, Path=Value, StringFormat={}{0:0}%}"
                                               FontWeight="Bold" Foreground="#667EEA" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Power Management Section -->
                    <Border Background="#F9FAFB" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="🔋 إدارة الطاقة" FontSize="16" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,15"/>

                            <!-- Idle Time -->
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="وقت السكون (دقائق)" FontSize="14" Foreground="#374151" Margin="0,0,0,8"/>
                                <TextBox x:Name="IdleTimeTextBox" Style="{StaticResource ModernTextBox}" Text="3"/>
                            </StackPanel>

                            <!-- Lock Device -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="قفل الجهاز" FontSize="14" Foreground="#374151"/>
                                    <TextBlock Text="تمكين قفل لوحة المفاتيح" FontSize="12" Foreground="#9CA3AF"/>
                                </StackPanel>
                                <CheckBox x:Name="LockDeviceToggle" Grid.Column="1" Style="{StaticResource ToggleSwitch}" VerticalAlignment="Center"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Device Operations Section -->
                    <Border Background="#FEF3F2" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="⚠️ عمليات الجهاز" FontSize="16" FontWeight="Bold" Foreground="#DC2626" Margin="0,0,0,15"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Button Grid.Column="0" Content="🔄 إعادة تشغيل الجهاز"
                                        Style="{StaticResource CancelButton}" Margin="0,0,5,0"
                                        Click="RestartDevice_Click"/>
                                <Button Grid.Column="1" Content="🔌 إيقاف تشغيل الجهاز"
                                        Style="{StaticResource CancelButton}" Margin="5,0,0,0"
                                        Click="PowerOffDevice_Click"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Action Buttons -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Content="إلغاء"
                                Style="{StaticResource CancelButton}"
                                Click="CloseButton_Click"
                                Margin="0,0,10,0"/>

                        <Button Grid.Column="1" Content="💾 حفظ الإعدادات"
                                Style="{StaticResource SuccessButton}"
                                Click="SaveSettings_Click"
                                Margin="10,0,0,0"/>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
