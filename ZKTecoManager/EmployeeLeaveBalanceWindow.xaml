<Window x:Class="ZKTecoManager.EmployeeLeaveBalanceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="أرصدة الإجازات - Leave Balances" Height="650" Width="750"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="CanResize"
        MinWidth="650"
        MinHeight="500"
        FlowDirection="RightToLeft"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style x:Key="BalanceCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.1" ShadowDepth="2" BlurRadius="10"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="#F3F4F6">
        <Border.Effect>
            <DropShadowEffect Color="#1F2937" Opacity="0.2" ShadowDepth="5" BlurRadius="20"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="55"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" CornerRadius="15,15,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#8B5CF6" Offset="0"/>
                        <GradientStop Color="#7C3AED" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="20,0" FlowDirection="LeftToRight">
                        <TextBlock Text="📊" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="أرصدة إجازات الموظف" FontSize="17" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Button Content="✕" Click="CloseButton_Click"
                            HorizontalAlignment="Left" VerticalAlignment="Center"
                            Margin="12,0" Width="32" Height="32"
                            Background="Transparent" Foreground="White"
                            BorderThickness="0" Cursor="Hand" FontSize="14"/>
                </Grid>
            </Border>

            <!-- Employee Info -->
            <Border Grid.Row="1" Background="White" Padding="20,15">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="👤" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock x:Name="EmployeeBadgeText" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock Text=" - " FontSize="16" VerticalAlignment="Center"/>
                        <TextBlock x:Name="EmployeeNameText" FontSize="16" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" FlowDirection="LeftToRight">
                        <TextBlock Text="السنة:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox x:Name="YearComboBox" Width="100" Height="32" SelectionChanged="YearComboBox_SelectionChanged"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Balance Cards Grid -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="10">
                <StackPanel>
                    <!-- Balance Cards -->
                    <ItemsControl x:Name="BalanceCardsControl">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel HorizontalAlignment="Center"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource BalanceCard}" Width="320">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- Leave Type Name -->
                                        <StackPanel Grid.Row="0" Margin="0,0,0,15">
                                            <TextBlock Text="{Binding LeaveTypeNameAr}" FontSize="16" FontWeight="Bold" Foreground="#1F2937"/>
                                            <TextBlock Text="{Binding LeaveTypeNameEn}" FontSize="12" Foreground="#6B7280"/>
                                        </StackPanel>

                                        <!-- Balance Details -->
                                        <Grid Grid.Row="1" Margin="0,0,0,15">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="المستحق:" FontSize="12" Foreground="#6B7280"/>
                                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding TotalAccrued, StringFormat=N2}" FontSize="12" FontWeight="SemiBold" Foreground="#059669"/>

                                            <TextBlock Grid.Row="1" Grid.Column="0" Text="المستخدم:" FontSize="12" Foreground="#6B7280" Margin="0,4,0,0"/>
                                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding UsedDays, StringFormat=N2}" FontSize="12" FontWeight="SemiBold" Foreground="#EF4444" Margin="0,4,0,0"/>

                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="المرحل:" FontSize="12" Foreground="#6B7280" Margin="0,4,0,0"/>
                                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding CarriedOver, StringFormat=N2}" FontSize="12" FontWeight="SemiBold" Foreground="#3B82F6" Margin="0,4,0,0"/>

                                            <TextBlock Grid.Row="3" Grid.Column="0" Text="التعديل:" FontSize="12" Foreground="#6B7280" Margin="0,4,0,0"/>
                                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding ManualAdjustment, StringFormat=N2}" FontSize="12" FontWeight="SemiBold" Foreground="#F59E0B" Margin="0,4,0,0"/>
                                        </Grid>

                                        <!-- Remaining Balance -->
                                        <Border Grid.Row="2" Background="{Binding CardBackground}" CornerRadius="8" Padding="15,12" Margin="0,0,0,10">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <TextBlock Text="المتبقي: " FontSize="14" Foreground="{Binding CardForeground}"/>
                                                <TextBlock Text="{Binding RemainingDays, StringFormat=N2}" FontSize="22" FontWeight="Bold" Foreground="{Binding CardForeground}"/>
                                                <TextBlock Text=" يوم" FontSize="14" Foreground="{Binding CardForeground}"/>
                                            </StackPanel>
                                        </Border>

                                        <!-- Adjust Button (only for superadmin) -->
                                        <Button Grid.Row="3" Content="تعديل الرصيد" Click="AdjustBalance_Click"
                                                Tag="{Binding}"
                                                Visibility="{Binding AdjustButtonVisibility}"
                                                Height="32" Cursor="Hand"
                                                Background="#F59E0B" Foreground="White" BorderThickness="0">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Hourly Accumulator Card -->
                    <Border Style="{StaticResource BalanceCard}" HorizontalAlignment="Center" Width="350" Margin="8,20,8,8">
                        <StackPanel>
                            <TextBlock Text="الساعات المتراكمة" FontSize="16" FontWeight="Bold" Foreground="#1F2937" TextAlignment="Center"/>
                            <TextBlock Text="Accumulated Hours" FontSize="12" Foreground="#6B7280" TextAlignment="Center" Margin="0,2,0,15"/>

                            <Border Background="#FEF3C7" CornerRadius="8" Padding="20,15">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock x:Name="HourlyAccText" Text="0" FontSize="32" FontWeight="Bold" Foreground="#D97706"/>
                                        <TextBlock Text=" ساعة" FontSize="16" Foreground="#D97706" VerticalAlignment="Bottom" Margin="5,0,0,5"/>
                                    </StackPanel>
                                    <TextBlock Text="(7 ساعات = يوم واحد يخصم من الاعتيادية)" FontSize="11" Foreground="#92400E" TextAlignment="Center" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>

                            <Grid Margin="0,15,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                    <TextBlock Text="إجمالي المحول" FontSize="11" Foreground="#6B7280" TextAlignment="Center"/>
                                    <TextBlock x:Name="TotalConvertedText" Text="0" FontSize="14" FontWeight="SemiBold" TextAlignment="Center"/>
                                    <TextBlock Text="ساعة" FontSize="10" Foreground="#9CA3AF" TextAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                    <TextBlock Text="أيام مخصومة" FontSize="11" Foreground="#6B7280" TextAlignment="Center"/>
                                    <TextBlock x:Name="TotalDaysDeductedText" Text="0" FontSize="14" FontWeight="SemiBold" TextAlignment="Center"/>
                                    <TextBlock Text="يوم" FontSize="10" Foreground="#9CA3AF" TextAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="3" Background="White" CornerRadius="0,0,15,15" Padding="20,12">
                <Button Click="CloseButton_Click" Height="38" Padding="30,0" Cursor="Hand"
                        HorizontalAlignment="Left"
                        Background="#6B7280" Foreground="White" BorderThickness="0" FontSize="14" FontWeight="SemiBold">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <TextBlock Text="إغلاق"/>
                </Button>
            </Border>

            <!-- Loading Overlay -->
            <Border x:Name="LoadingOverlay" Grid.RowSpan="4" Background="#80FFFFFF" Visibility="Collapsed">
                <TextBlock Text="جاري التحميل..." FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
        </Grid>
    </Border>
</Window>
