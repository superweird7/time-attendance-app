<Window x:Class="ZKTecoManager.AddExceptionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Exception Type" 
        Height="268" 
        Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="NoResize">

    <Border Background="White" 
            CornerRadius="10" 
            BorderBrush="#2196F3" 
            BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" 
                    Background="#2196F3" 
                    CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <TextBlock Text="إضافة نوع استثناء جديد" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           VerticalAlignment="Center" 
                           HorizontalAlignment="Center"/>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="20">
                <TextBlock Text="اسم الاستثناء:" 
                           FontSize="14" 
                           FontWeight="SemiBold" 
                           Margin="0,0,0,5"/>
                <TextBox x:Name="ExceptionNameTextBox" 
                         Height="35" 
                         FontSize="14"
                         Padding="8"
                         FlowDirection="RightToLeft"
                         VerticalContentAlignment="Center"/>

                <TextBlock Text="مثال: غياب، إجازة، تأخير، " 
                           FontSize="11" 
                           Foreground="Gray" 
                           Margin="0,5,0,0" RenderTransformOrigin="0.813,0.504" Height="24" Width="107"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center" 
                        Margin="10">
                <Button x:Name="SaveButton" 
                        Content="حفظ" 
                        Width="120" 
                        Height="27" 
                        FontSize="14" 
                        FontWeight="Bold"
                        Background="#4CAF50" 
                        Foreground="White" 
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="SaveButton_Click"
                        Margin="5">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="5"
                                                BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#45A049"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button x:Name="CancelButton" 
                        Content="إلغاء" 
                        Width="120" 
                        Height="26" 
                        FontSize="14" 
                        FontWeight="Bold"
                        Background="#F44336" 
                        Foreground="White" 
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="CancelButton_Click"
                        Margin="5">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="5"
                                                BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#D32F2F"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
